version: 0.2

phases:
  pre_build:
    runtime-versions:
      python: 3.8
    commands:
      - pip3 install flake8 pytest
  build:
    commands:
      - pytest -v
  post_build:
    commands:
      - echo Build completed on `date`
artifacts:
  files:
    - '**/*'
  name: blackjack-latest